<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tweiy.mapper.ClazzMapper">
    <select id="list" resultType="com.tweiy.pojo.Clazz">
        select c.*, e.name as masterName, (
            case 
                when now() &lt; c.begin_date then '未开班'
                when now() &gt; c.end_date then '已结班'
                else '在读中'
         end ) as status from clazz as c left join emp as e on c.master_id = e.id
          <where>
                <if test="name != null">
                    c.name like concat('%', #{name}, '%')
                </if>
                <if test="begin != null and end != null">
                    and c.end_date between #{begin} and #{end}
                </if>
          </where>
    </select>
</mapper>